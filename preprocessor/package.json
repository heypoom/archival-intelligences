{
  "name": "preprocessor",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "analyze": "bun run process.ts",
    "generate": "bun run generate.ts",
    "generate-force": "bun run generate.ts --force",
    "stats": "bun run batch-generator.ts --stats-only"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "p-queue": "^8.1.0"
  }
}
